---
description: 每次对话的底层步骤：目标-约束-验收
alwaysApply: true
---

# 对话底层步骤：目标-约束-验收

本项目中，每次对话必须遵循以下三步，作为不可变更的底层流程：

## 1. 目标 (Goal)

先明确本次对话要达成什么。用户提出需求时，先提炼并确认目标。

## 2. 约束 (Constraints)

在动手前，明确限制条件：技术栈、格式、性能、兼容性、不可破坏的既有逻辑等。

## 3. 验收 (Acceptance)

完成前，对照目标与约束，确认是否满足。未通过则继续迭代，通过则结束。

---

**重要**：此流程为本项目固定规范，不因 token 更新或上下文变化而改变。
